---
layout: layout-2-1-columns
---

<% content_for :breadcrumbs do %>
  <%= partial "partial-breadcrumbs-business" %>
<% end %>

<% content_for :column1 do %>

 <header class="page-header">
    <hgroup>
      <h1>Check land details</h1>
    </hgroup>
  </header>

  <p>Check the land parcels to make sure they show the land as it will be on 12 May 2015 (this is the date that scheme payments are based on).</p>


  <table class="parcels js-row-link">
    <thead>
      <tr>
        <th class="parcel-name">Name</th>
        <th class="parcel-id">ID</th>
        <th class="parcel-area">Area</th>
        <th class="parcel-status">Checked</th>
      </tr>
    </thead>
    <tbody>

      <% data.business.parcels.each do |f| %>
      <% statusClass = (f.checked ? "checked" : "not-checked") %>
      <% statusName = (f.checked ? "Yes" : "No") %>
      <tr class="<%= statusClass %>">
        <td class="parcel-name">
          <a href="/<%= f.id %>">
            <%= (f.name ? f.name : "Untitled") %>
          </a>
        </td>
        <td class="parcel-id"><%= f.id %></td>
        <td class="parcel-area"><%= f.area %>ha</td>
        <td class="parcel-status"><%= statusName %></td>
      </tr>
      <% end %>

    </tbody>
  </table>

  <p></p>

<% end %>

<% content_for :column2 do %>

  <%= partial "partial-map", :locals => { :map => 'business-1-checked'} %>

  <div class="font-xsmall">
    <h2 class="heading-small">Land details</h2>

    <ul>
      <li>Area: <%= data.business.area %>ha</li>
      <li>SBI: <%= data.business.sbi %></li>
    </ul>

    <ul>
      <li><a href="add-land-parcel">Add land</a></li>
      <li><a href="add-land-parcel">Remove land</a></li>
    </ul>
  </div>

<% end %>
